# Leçons — ComRoster

Format : `[date] | ce qui a mal tourné | règle pour l'éviter`

[2026-06-19] | pytest ne trouvait pas le module `comroster` (ModuleNotFoundError dans conftest) | Tout projet Python à la racine sans packaging : créer un `pyproject.toml` avec `[tool.pytest.ini_options] pythonpath = ["."]` dès P0, sinon les imports échouent.
[2026-06-19] | TemplateNotFound : Flask cherche `comroster/templates/` mais les templates sont à la racine du projet | Quand templates/ et static/ sont hors du package, instancier `Flask(__name__, template_folder="../templates", static_folder="../static")`.
[2026-06-19] | Smoke test HTTP local : login/publish renvoyaient 400 (CSRF) car `SESSION_COOKIE_SECURE=True` empêche le cookie de session de circuler sur HTTP non-TLS | Comportement CORRECT en prod (HTTPS). Pour tester en local sans TLS, lancer avec `FLASK_DEBUG=true` (désactive SECURE) ou passer par le test client pytest (CSRF off).
[2026-06-20] | Layout app-shell (header/footer position:fixed + conteneur central flex:1 overflow:auto) : scroll cassé sur admin ET auto-scroll JS inactif sur display | Cause racine commune : `body` avec `min-height:100vh` mais pas `height:100vh`. Le conteneur central n'est jamais borné → pas d'overflow interne, c'est la fenêtre qui scrolle. TOUJOURS borner `body` (ou la page) à `height:100vh; overflow:hidden` pour que les enfants `flex:1; overflow:auto` scrollent en interne.
[2026-06-21] | Élément togglé par l'attribut `hidden` mais avec une règle CSS `display:flex/inline-flex` → l'attribut hidden est écrasé, l'élément reste affiché (ex. assistant+dashboard+étapes tous visibles, barre de sélection collée). Erreur commise 2 fois. | RÈGLE D'OR : ajouter une fois pour toutes `[hidden] { display: none !important; }` au reset CSS. Ne jamais poser un `display` inconditionnel sur un élément piloté par `hidden` (ou utiliser `:not([hidden])`).
[2026-06-22] | Pastilles temps réel (état antenne) ne disparaissaient pas à la déconnexion sans rafraîchir : elles dépendaient d'un poll périodique (5 s) jamais déclenché par l'action utilisateur. | Tout état d'UI alimenté par un polling doit être rafraîchi IMMÉDIATEMENT après l'action qui le modifie (connect/disconnect/import) — appeler la fonction de poll juste après l'action, sans attendre le prochain tick. Ne jamais compter sur l'intervalle seul pour refléter une action explicite.
[2026-06-22] | Un test existant imposait « échouer bruyamment » sur fichier d'état corrompu (load_draft devait lever). Pour un produit livré en boîtier, ça brickerait le client. | Politique APPLIANCE = fail-safe, pas fail-loud : récupérer (sauvegarde .bak puis état vide) AU LIEU de planter, mais TOUJOURS journaliser (warning/error) pour ne pas masquer. Quand on change une politique, mettre à jour le test qui encodait l'ancien contrat plutôt que le contourner.
[2026-07-06] | Le recover permettait un mot de passe vide : la règle de longueur n'existait que sur le chemin setup | Une politique de validation (longueur mdp, format…) doit être appliquée sur TOUS les chemins qui écrivent la même donnée (setup + recover + change). Avant de valider, lister les chemins d'écriture.
[2026-07-06] | Rate-limit par IP inopérant derrière nginx : get_remote_address voit 127.0.0.1 pour tout le monde (compteur partagé → un attaquant verrouille le login de tous) | Toute logique basée sur l'IP client doit prévoir le cas reverse-proxy : ProxyFix opt-in par env var (jamais actif par défaut, sinon spoofable en direct) + le documenter dans la conf du proxy.
[2026-07-06] | `except Exception: return "DISPLAY OK"` (vestige de dev) masquait tout bug de template en prod | Le fail-safe appliance s'applique aux DONNÉES externes (fichiers corrompus, réseau), JAMAIS aux bugs de code : pas de catch-all autour d'un render/logique interne. Un vestige de debug qui avale les exceptions doit sauter avant livraison.
[2026-07-06] | Chaque flux SSE occupe un thread gthread en continu : 8 threads = 8 displays et le serveur entier ne répond plus (même /healthz) | Pour tout endpoint streaming sur un pool de threads borné : imposer un cap applicatif (503 + Retry-After) STRICTEMENT inférieur au nombre de threads, et vérifier que le client sait retenter après une erreur HTTP.
[2026-07-06] | Le lock d'écriture atomique ne protégeait pas le cycle load→mutate→save : deux PATCH simultanés se perdaient (last-write-wins) | L'atomicité d'écriture ≠ l'atomicité de transaction. Tout read-modify-write partagé doit être sérialisé sur toute sa durée (décorateur lock au niveau handler), pas seulement sur l'écriture finale.
[2026-07-07] | CSP stricte activée alors que display.html contenait un bloc <style> de 128 lignes → mise en page de l'écran TV totalement cassée. Le grep de contrôle (`<style`) avait renvoyé « 0 matches » À TORT (sortie mangée par le hook rtk) et j'ai conclu sur ce faux négatif ; les e2e n'ont rien vu car ils testent le contenu, pas le rendu | (1) Ne JAMAIS conclure sur un résultat négatif d'un seul outil potentiellement filtré : recouper avec une 2e méthode (Read direct, second pattern). (2) Tout changement de CSP se valide par un rendu réel : screenshot + console navigateur (les violations CSP y sont explicites), pas seulement par les en-têtes et les tests DOM.
[2026-07-10] | Mode 2 Pi : les services systemd --user (kiosk, agent afficheur) ne peuvent PAS lire /etc/comroster.env (root, chmod 600) — un EnvironmentFile pointant dessus échoue silencieusement. | Pour un service --user, ne jamais compter sur un EnvironmentFile root 600 : injecter les variables non-secrètes directement dans l'unité via `Environment=` (setup-pi.sh connaît la valeur à la génération), ou les mettre dans un fichier lisible par l'utilisateur. Les secrets restent au service système (root).
[2026-07-10] | Kiosk ne démarre pas au boot sur Raspberry Pi OS Bookworm : le service --user est WantedBy=graphical-session.target, mais le compositeur Wayland labwc (défaut Bookworm, même sur Pi 3) n'émet JAMAIS graphical-session.target → service enabled mais jamais déclenché (Active: inactive, aucun log). Le bureau s'affiche pourtant (autologin OK). | Sous Wayland/labwc, ne pas compter sur graphical-session.target pour lancer un service --user graphique. Amorcer depuis l'autostart du compositeur (~/.config/labwc/autostart, copié depuis /etc/xdg/labwc/autostart pour préserver le bureau) : `systemctl --user import-environment WAYLAND_DISPLAY` puis `systemctl --user start <service>`. Bonus Chromium/Wayland/Pi 3 : `--use-gl=egl` est obsolète (→ « gl=none », GPU KO, rendu software lent) → utiliser `--ozone-platform=wayland` ; kiosk = ajouter `--password-store=basic` (pas de popup trousseau) et `--disable-features=Translate,TranslateUI` (pas de popup traduction).
[2026-07-10] | Kiosk Bookworm/labwc, tentative « écran noir » : (1) créer ~/.config/labwc/autostart ne REMPLACE pas /etc/xdg/labwc/autostart — labwc exécute les DEUX ; le bureau (pcmanfm-pi + wf-panel-pi) lancé par le global continuait de s'afficher. (2) retirer `splash` de cmdline.txt ne coupe pas le logo Raspberry : c'est Plymouth (service « static », non désactivable par disable) → il faut `plymouth.enable=0`. (3) `sed 's|…(a|b)…|…|'` : le délimiteur `|` entre en conflit avec l'alternation ERE `|` → « bad flag » (casse aussi sur GNU sed) → utiliser un autre délimiteur (@). | Pour un vrai kiosk labwc : neutraliser le bureau dans le GLOBAL /etc/xdg/labwc/autostart (commenter pcmanfm-pi/wf-panel-pi, garder kanshi/lxsession), pas seulement poser un autostart local. Splash Plymouth → `plymouth.enable=0`. Et ne jamais utiliser un métacaractère regex comme délimiteur sed.
[2026-07-10] | Bascule Desktop+labwc → Lite+cage : l'approche « kiosk pro ». cage = compositeur Wayland mono-app, lancé en service SYSTÈME (pas --user) via PAMName=login + TTYPath=/dev/tty1 (session logind → accès seat/DRM), Conflicts=getty@tty1, utilisateur dans groupes video/render/input. Beaucoup plus simple que labwc (plus de bureau à neutraliser, d'autostart, de linger, d'autologin desktop). | Pour un kiosk appliance, viser Pi OS Lite + cage dès le départ plutôt que Desktop : moins de couches, boot plus rapide, rien à désactiver. ⚠️ NON TESTABLE sans matériel (seat/DRM/tty/permissions) — écrire d'après les patterns connus mais prévoir 1-2 itérations sur le vrai Pi. Nettoyer les cadavres à la bascule : services --user, autostart labwc, uninstall, et toute la doc (Desktop→Lite, systemctl --user→system).
[2026-07-12] | CI rouge sur le job lint : shellcheck local (0.11.0, brew) ne signalait rien, mais la CI (ubuntu-24.04, apt = shellcheck 0.9.0) échouait sur SC2015 (`A && B || C` pas équivalent à if-then-else) → un lint « vert » en local mais rouge en CI. | Un linter doit être testé avec la MÊME version que la CI. shellcheck varie beaucoup entre versions. Valider avant push avec la version du CI : `docker run koalaman/shellcheck:v0.9.0` ou le binaire officiel de la release. Éviter le pattern `cmd && action || true` dans les scripts (préférer un `if`).
[2026-07-21] | Bouton « Redémarrer » sans effet ET sans message : `subprocess.Popen(["sudo","systemctl","reboot"])` en fire-and-forget — le code retour n'était jamais lu, donc l'API répondait `ok:True` même quand sudo refusait (droit manquant). Échec 100 % silencieux. Sans `-n`, sudo pouvait en plus BLOQUER en attendant un mot de passe (aucun TTY dans un service systemd). | Toute commande externe dont le résultat conditionne la réponse utilisateur doit être ATTENDUE (`subprocess.run`) et son code retour remonté au client. Pour sudo hors TTY : toujours `sudo -n` (échoue au lieu de bloquer). Corollaire côté navigateur : pour une action qui coupe volontairement la connexion (reboot, changement d'IP), une requête échouée n'est PAS une erreur — seule une réponse d'erreur EXPLICITE du serveur en est une, sinon on affiche de faux échecs.
[2026-07-21] | `setup-pi.sh` créait `/etc/sudoers.d/comroster-reboot` (privilège root) mais `uninstall-pi.sh` ne le supprimait pas → privilège root orphelin après désinstallation. | Tout ce qu'un installeur écrit HORS du répertoire applicatif (sudoers, units systemd, fichiers /etc, sauvegardes de /boot) doit recevoir sa ligne de suppression dans le désinstalleur **dans le même commit**. Revue systématique : `grep -oE '/(etc|boot|opt)/[^ "]+' setup-pi.sh` puis vérifier chaque chemin dans uninstall-pi.sh.
[2026-07-21] | `deploy/comroster.service` (User=comroster, /opt/comroster) contredisait l'unit réellement installé, que `setup-pi.sh` GÉNÈRE avec `User=${SUDO_USER:-pi}` et le chemin du dépôt. J'ai donné à Nathan une commande sudoers avec le mauvais utilisateur en me fiant à ce fichier. Bonus piège : ce modèle contient `NoNewPrivileges=true`, qui **casse sudo** (donc les boutons Redémarrer / Appliquer maintenant). | Un fichier de conf du dépôt qui n'est PAS déployé tel quel doit le dire en gros dans son en-tête (modèle de référence + où est la vraie valeur). Pour connaître une valeur réelle sur une machine, interroger le système (`systemctl show -p User --value <unit>`), jamais un fichier du dépôt.
[2026-07-22] | Une revue EXHAUSTIVE (tout le code lu, pas un échantillon) a révélé ce que l'ajout incrémental laisse filer : du code mort (`merge_beltpacks`, 0 usage, maintenu en vie par son seul test), une incohérence front/back sur la politique de mot de passe (front 8 / back 4), et un état partagé thread↔requêtes sans verrou. Rien de critique, mais invisible sans lecture complète. | Périodiquement (ou sur demande « qualité »), lire TOUT le code, pas juste échantillonner : le grep « usage d'une fonction hors sa def et hors tests » débusque le code mort ; vérifier qu'une même règle (longueur mdp, format) est affichée ET imposée IDENTIQUEMENT côté front et back ; repérer tout attribut d'instance muté depuis un thread de fond ET une requête → verrou (réseau hors verrou). Annoncer honnêtement ce qui est lu en profondeur vs survolé.
[2026-07-22] | 103 warnings pytest « ignorés comme du bruit ». En les catégorisant avant de les museler, la majorité (86) étaient des `ResourceWarning: unclosed` = vrais défauts : `open()`/`json.load(open())` sans `with` dans les tests (fd fichiers qui fuient jusqu'au GC — d'où l'attribution trompeuse au test lourd qui déclenche la collecte, pas au coupable), et `HTTPServer.shutdown()` sans `server_close()` (le socket d'écoute reste ouvert). Les museler en masse aurait caché ces fuites. | Ne JAMAIS museler des warnings en bloc : les catégoriser d'abord (`pytest -W default | grep -i warning | sort | uniq -c`). Un `ResourceWarning: unclosed` se corrige à la RACINE (`with` pour tout descripteur, `server_close()` après `shutdown()`), jamais par filtre. Museler n'est légitime que pour un choix ASSUMÉ, et alors en le rendant explicite dans le code (ex. `Limiter(storage_uri="memory://")`) plutôt que par un `filterwarnings` aveugle.
[2026-07-22] | Le bouton « Redémarrer » restait cassé même après le fix précédent (Popen→run, sudoers ajouté) : l'unit systemd RÉEL généré par setup-pi.sh contenait `NoNewPrivileges=true`, qui interdit toute élévation via sudo → `sudo -n systemctl reboot` échoue TOUJOURS. Cause racine manquée car on avait corrigé le code applicatif et le sudoers, mais jamais relu l'unité générée. | Quand un correctif ne lève pas le symptôme, remonter TOUTE la chaîne d'exécution, unité systemd comprise : les durcissements (NoNewPrivileges, ProtectSystem, CapabilityBoundingSet) peuvent neutraliser sudo/reboot. Un droit accordé par sudoers est inutile si le service interdit l'élévation.
[2026-07-23] | Bouton d'agrandissement du témoin d'aperçu : je l'avais stylé via `.preview-tile-hit` (1 classe), mais `.admin-page button` (1 classe + 1 élément) est PLUS spécifique et lui réimposait `height: 2rem`, un fond et une bordure. Mon `inset: 0` était donc écrasé : au lieu d'un bouton transparent couvrant toute la vignette, une pilule visible de 32 px en haut, seule zone cliquable. Pire, le test e2e passait — `page.click("#preview-btn")` vise le CENTRE DE L'ÉLÉMENT, donc il cliquait la pilule et validait un comportement cassé. Signalé par Nathan, deux fois. | (1) Dans une feuille qui contient des règles génériques sur un ÉLÉMENT (`.page button`, `.page input`), une classe seule ne suffit jamais à les surcharger : écrire `.page button.ma-classe`, et le vérifier par `getComputedStyle`, pas à l'œil. (2) Un test qui clique un SÉLECTEUR D'ÉLÉMENT ne teste jamais la géométrie de cet élément : pour valider « toute la zone est cliquable », cliquer des COORDONNÉES (un coin du conteneur) via `page.mouse.click`, jamais l'élément. (3) Corollaire des leçons du jour : après correction, rétablir le défaut et vérifier que le test échoue — fait ici, il échoue bien.
[2026-07-23] | Apparences `lineaire`/`grille` : le numéro de beltpack apparaissait ~6 px au-dessus du rôle. Cause : en sortant le voyant temps réel (`.bp-dot`) de sa pastille, je l'avais repassé de `position: absolute` (valeur de `basique`) à `static`. Sans antenne connectée il n'a AUCUN fond — donc invisible — mais il occupe toujours ~11 px dans le flux sous le chiffre ; la pastille étant centrée dans la ligne, le numéro remontait de la moitié exactement. Bug invisible sur mes captures de validation, repéré par Nathan à l'usage. | Faire passer un élément de `absolute` à `static` le réintroduit dans le flux : s'il peut être INVISIBLE (pas de fond, pas de contenu) tout en restant présent, il décale silencieusement l'alignement de ses voisins. Avant de déplacer un tel élément, se demander « qu'occupe-t-il quand il ne montre rien ? ». Et ne jamais valider un alignement à l'œil sur une capture : mesurer — c'est ce qui a donné la cause en une passe, et ça devient un test de non-régression gratuit. ⚠️ CORRIGÉ le même jour : j'avais d'abord comparé les CENTRES DE BOÎTES, conclu « 0,0 px » et annoncé le bug résolu ; Nathan voyait toujours un décalage. Voir la leçon suivante — la bonne métrique est la ligne de base.
[2026-07-23] | Après avoir corrigé le décalage du numéro de beltpack, j'ai mesuré l'écart des CENTRES DE BOÎTES, trouvé 0,00 px, et annoncé que c'était réglé. Ça ne l'était pas : le chiffre est en `line-height: 1` et le rôle en `1.15`, or deux boîtes de rapports différents peuvent avoir le même centre avec des glyphes décalés. Une métrique fausse donne une confiance fausse — pire que pas de mesure, parce qu'elle clôt l'enquête. | Pour aligner du TEXTE, la métrique est la LIGNE DE BASE, jamais le centre de la boîte ni `top`/`bottom`. Côté CSS : `align-items: baseline` sur le conteneur flex. Côté mesure : insérer une sonde `<span>` en `display:inline-block; width:0; height:0` dans le texte — sa ligne de base est son bord inférieur, donc son `rect.top` donne celle du texte hôte. Règle générale : avant de conclure sur un chiffre, vérifier qu'il mesure bien ce que l'utilisateur PERÇOIT (ici des glyphes, pas des rectangles).
[2026-07-23] | Passe de doc du lot « apparences » : le README annonçait « mot de passe admin (8 caractères min.) » alors que `MIN_PASSWORD_LENGTH = 4` depuis le 2026-07-06, et « ajouter les personnes (nom + n° beltpack + rôle) » alors que le champ nom n'existe plus NI dans l'admin NI dans le modèle. Deux affirmations fausses, invisibles parce que personne ne relit un README à côté du code. | Une passe de doc n'est pas une relecture de style : chaque affirmation VÉRIFIABLE (longueurs, champs de formulaire, routes, valeurs par défaut, noms de fichiers) se contrôle par un grep dans le code AVANT d'être laissée en place. C'est le corollaire de la leçon 2026-07-06 (« une même règle doit être imposée sur tous les chemins ») : la documentation est un de ces chemins, et c'est celui qui dérive en silence.
[2026-07-23] | Deux pièges de vérification e2e sur le même lot : (1) `assert errors == []` sur une collecte console est CREUX si le collecteur ne s'arme jamais — l'assertion passe pour de mauvaises raisons ; (2) `wait_for_selector("#display-grid")` a échoué sur une grille sans groupe, Playwright attendant `visible` par défaut alors qu'un conteneur vide a une hauteur nulle (le message « resolved to hidden » ne le dit pas clairement). | (1) Toute assertion NÉGATIVE (« aucune erreur console », « zéro EventSource ouvert ») doit être validée une fois contre un cas qui échoue volontairement, sinon elle ne prouve rien — même esprit que la leçon 2026-07-07 sur les faux négatifs. (2) Pour attendre un conteneur potentiellement vide, utiliser `state="attached"`, jamais le `visible` implicite.
[2026-07-22] | Backticks dans un heredoc NON quoté (`cat <<EOF`) : `` `cmd` `` est exécuté comme substitution de commande AU MOMENT de générer le fichier (ici dans un commentaire d'unit → un `sudo …` aurait tourné pendant l'install). Détecté par shellcheck (SC2006) avant commit. | Dans un heredoc non quoté, tout `$(...)`, backtick et `$var` est expansé, commentaires inclus. Pour du texte littéral : quoter le délimiteur (`<<'EOF'`) ou bannir backticks/`$`. Toujours passer les scripts modifiés à shellcheck avant de committer.
[2026-07-23] | Le témoin d'aperçu affichait 3 colonnes là où l'écran de régie en montrait 4, et ne défilait pas. Deux causes que j'avais introduites sans les voir : (1) l'iframe était rendue à 1280x720 alors que `repeat(auto-fit, minmax(340px, 1fr))` fait dépendre le nombre de colonnes de la LARGEUR EN PIXELS — un aperçu à une résolution différente de la cible ne peut pas être fidèle, par construction ; (2) le mode aperçu coupait le défilement automatique « pour économiser », ce qui masquait précisément la question à laquelle l'aperçu sert à répondre (« est-ce que tout tient à l'écran ? »). | Un aperçu WYSIWYG doit être rendu à la RÉSOLUTION RÉELLE de la cible, pas à une taille commode : toute mise en page responsive (auto-fit, media queries, container queries) rend l'aperçu faux à toute autre largeur. Corollaire : quand on désactive des comportements dans un mode « léger », lister ce que chaque désactivation retire à l'UTILISATEUR, pas seulement au serveur — ici l'économie portait sur la seule fonction qui donnait sa valeur à l'aperçu. Et la résolution ne doit être écrite qu'à un endroit (le JS déduit l'échelle de `offsetWidth`, il ne redéclare pas 1920).
[2026-07-23] | Le témoin épinglé en bas de la barre latérale mangeait ~165 px sur 244 px de large (vignette 16/9) : un quart du menu, qu'il fallait scroller pour atteindre les réglages. Défaut de conception que je n'avais pas anticipé en le posant là — je l'avais évalué isolément (« ça tient »), jamais dans le budget vertical de la barre. | Tout élément PERMANENT ajouté à un conteneur borné (barre latérale, en-tête, pied) se juge à son coût dans le budget de ce conteneur, pas à sa taille propre : mesurer le pourcentage pris et ce qui devient inaccessible sans défilement. Quand un élément est un témoin passif (il informe, on n'interagit pas avec), la surimpression repliable coûte 0 à la mise en page — et l'état de repli se persiste, sinon il se rouvre à chaque rechargement.
[2026-07-25] | Revue de Nathan après la refonte : 10 défauts d'ARCHITECTURE D'INFORMATION que la fidélité visuelle ne voyait pas — deux boutons Intercom, aperçu accessible à deux endroits, « Système » qui ouvrait le même dialogue que « Réseau », vues inutiles (« Tous », « Non affectés » = doublon de la réserve visible), « grep » jargonneux, « Historique » ambigu, onglet « Journal » qui n'était pas un journal. Copier la maquette ne suffit pas : la maquette montrait des ONGLETS et des CHIPS, pas la carte des fonctions. | Règle « un bouton par fonction » : avant d'ajouter un accès, chercher si la fonction en a déjà un ; à la revue, lister chaque fonction et compter ses accès (doit faire 1). Un élément d'état qui est aussi cliquable (chip antenne) COMPTE comme l'accès. Nommer par ce que ça FAIT pour l'utilisateur (« Publications », « Filtrer »), pas par le terme technique (« Historique », « grep »). Une vue/option qui égale l'état par défaut ou duplique un panneau visible est du bruit : la supprimer.
[2026-07-25] | Les raccourcis AFFICHÉS dans l'interface (⌘↵ sur le bouton Publier, « / » sur le filtre, ⌘K sur la recherche réserve) n'étaient pas implémentés — le code n'écoutait que ⌘S, jamais montré. Trois `<kbd>` décoratifs = trois promesses non tenues, signalées à l'usage. | Chaque raccourci AFFICHÉ (`<kbd>`, infobulle) est un contrat : au moment où on pose le visuel, câbler le handler DANS LE MÊME COMMIT, et vérifier par un test navigateur (presser la touche, constater l'effet). À la revue : grep `<kbd>` et confronter chaque occurrence au listener correspondant.
[2026-07-25] | Trois pièges d'UX signalés à l'usage : (1) le toast « n° déjà utilisé » était ÉMIS mais INVISIBLE — un <dialog> modal vit dans le top layer du navigateur, AU-DESSUS de tout z-index ; un toast posé dans <body> ne peut pas passer devant. (2) L'auto-remplissage du nom (`if (known && !champ.value)`) se FIGEAIT au premier chiffre : taper « 2 » remplissait, continuer en « 22 » ne changeait plus rien. (3) La cascade d'animation, posée sur `.admin-block`, rejouait à CHAQUE re-rendu (donc à chaque frappe) puisque le rendu re-crée les nœuds. | (1) Tout overlay (toast, tooltip custom) doit être monté DANS le dialogue modal ouvert s'il y en a un (top layer), pas dans body — et jamais un échec muet : chaque refus dit quoi ET où (« existe déjà dans “Son” »). (2) Une proposition automatique doit rester VIVANTE tant que l'utilisateur n'a pas saisi le champ lui-même (drapeau `autofilled`, levé par l'input manuel) : suivre, remplacer, vider. (3) Une animation d'ARRIVÉE se déclenche par un état (attribut posé au premier rendu puis retiré), jamais par la simple présence des nœuds quand le rendu les re-crée.
[2026-07-25] | Les e2e attendaient un LIBELLÉ transitoire (« Brouillon enregistré ») comme signal de fin de sauvegarde ; en faisant de la chip un témoin véridique (« N en attente »/« À jour »), le libellé a disparu et 4 e2e ont cassé. | Ne jamais synchroniser un test sur un texte d'UI transitoire : attendre le CYCLE de l'état machine (data-state=syncing apparaît — tenu par le debounce, donc immanquable — puis disparaît). Corollaire : un drapeau d'état purement client (unpublished) ment après rechargement — le calculer aussi depuis les données serveur (comparaison d'horodatages brouillon/publié).
[2026-07-25] | Après DEUX passes de correctifs ciblés (polices, line-height, halo), la refonte restait infidèle : l'admin chargeait toujours main.css, et chaque écart colmaté en laissait d'autres (surfaces inversées #0E1119/#1E2330, chips en pilules, chrome des boutons, header fixed…). Porter une refonte sur une page qui garde la feuille globale est un combat perdu d'avance : on ne peut pas énumérer ce qu'on hérite. | Pour une refonte « identique à la maquette » : DÉCOUPLER — la page ne charge plus QUE sa feuille, reconstruite en prenant le CSS de la maquette comme source littérale (jetons, valeurs, géométrie), plus une section « app » pour ce que la maquette ne montre pas. Verrouiller le nouveau contrat par un test (`assert "main.css" not in html`) et ARCHIVER la maquette dans le dépôt (design/) — elle ne vivait que dans un scratchpad de session, la source de vérité aurait disparu avec lui.
[2026-07-25] | Refonte admin « conforme à la maquette » mais Nathan : « ça n'a rien à voir ». Mes captures paraissaient proches, pourtant trois VESTIGES de main.css contaminaient tout l'écran, invisibles en revue de code : (1) `--font-mono` jamais défini → tous les nombres/horloge/barre d'état tombaient en monospace SYSTÈME (Menlo) au lieu d'Inter ; (2) l'UI héritait de `--font-body` (Inter) alors que le preset retenu voulait Outfit ; (3) `line-height: 1.6` du body gonflait chaque ligne (35px au lieu de 31) et `body::before` posait un halo turquoise. Une variable CSS non définie ne casse pas bruyamment : `var(--font-mono, monospace)` retombe SILENCIEUSEMENT sur le fallback système. | Une « refonte » d'une page qui partage une feuille de style globale (main.css) hérite en douce de ses polices, line-height, halos, focus. Ne jamais juger la conformité à l'œil sur capture : (a) `getComputedStyle` sur les polices RÉELLES (fontFamily des nombres ET du texte), (b) MESURER les positions/hauteurs clés (row height, top des sections) contre la cible, (c) pixel-diff (PIL ImageChops) maquette vs réel — un décalage vertical constant ou une police système sautent aux yeux dans la diff, jamais dans l'aperçu. Vérifier que chaque `var(--x, fallback)` a bien un `--x` défini quelque part, sinon on rend le fallback sans le savoir.
[2026-07-26] | J'ai committé PAR-DESSUS des e2e cassés (6/13 rouges) : la commande `pytest ... | tail -1 && pytest tests/e2e | tail -1 && ruff | tail -1 && git commit` a enchaîné le commit alors que les e2e échouaient. Cause : `pytest | tail` renvoie le code retour de `tail` (toujours 0), pas de pytest → le `&&` ne bloque jamais. En plus, la cause de l'échec était que j'avais retiré `setStatus("Envoyé à l'affichage ✓")` sans mettre à jour les e2e qui attendaient ce texte exact. | Ne JAMAIS enchaîner un `git commit` derrière des `pytest | tail` avec `&&` : le pipe masque le code retour. Lancer les tests dans une commande SÉPARÉE, lire le résultat, PUIS committer manuellement. Corollaire (déjà vu le 2026-07-25) : changer un libellé d'UI casse les tests qui l'attendent en `wait_for_selector("text=…")` — chercher le texte dans les tests AVANT de le supprimer/renommer, et préférer synchroniser les e2e sur un état stable (chip « À jour ») plutôt que sur un texte transitoire.
[2026-07-26] | Pour vérifier que le projet démarrait, j'ai lancé un serveur de dev en tâche de fond sur le port 8080 et je l'y ai LAISSÉ. Les lancements de Nathan ont tous échoué en `Address already in use` — mais `run-dev.sh` affiche sa bannière (« ComRoster (dev) → http://… », « Debug mode: on ») AVANT l'échec de bind, donc ça ressemblait à un démarrage réussi qui « ne marche pas » sans raison. Pire : le `pkill -f "python app.py"` que je lui avais donné ne matchait RIEN — le binaire Homebrew sur macOS s'appelle `Python` (P majuscule), `pkill -f` est sensible à la casse, et il échoue EN SILENCE. Le port n'a donc jamais pu être libéré et chaque tentative rejouait le même échec. Trois échanges perdus, et Nathan a soupçonné son venv (intact). | (1) Tout processus de fond que je lance sur une RESSOURCE PARTAGÉE (port, socket, verrou) doit être arrêté dès la vérification finie, ou annoncé avec sa commande d'arrêt TESTÉE. (2) Ne jamais livrer un motif `pkill`/`pgrep` sans l'avoir vérifié d'abord : `pgrep -f "<motif>"` doit retourner les PID attendus AVANT de le donner — un `pkill` qui ne trouve rien ne dit rien. Préférer un motif indépendant du binaire (`pkill -f "app\.py"`) ou passer par le port (`lsof -ti tcp:PORT | xargs kill`). (3) Quand l'utilisateur dit « ça ne marche pas », EXIGER le message d'erreur avant de théoriser : ici « la bannière s'affiche » n'était pas une preuve de démarrage. Corollaire pour le projet : un script de lancement devrait vérifier le port AVANT d'afficher son URL, sinon il ment.
[2026-07-26] | `./run-dev.sh` échouait en `exec: python: not found` ALORS QUE le prompt affichait `(.venv)`. Cause : le projet avait été déplacé dans `INTERCOM/`, or un venv n'est PAS déplaçable — son chemin absolu est figé dans `activate`/`pyvenv.cfg` (le PATH recevait donc en tête un dossier inexistant) ET dans le shebang des 14 scripts console (`pytest`, `pip`, `gunicorn`, `flask`, `playwright`… → `bad interpreter`). L'indicateur `(.venv)` du prompt vient du NOM du dossier, jamais d'une vérification qu'il existe : il affirmait une activation réussie qui ne pointait nulle part. J'ai mis 3 échanges à trouver parce que je reproduisais depuis MON shell, qui avait déjà le bon `.venv/bin` dans son PATH — le chemin fantôme ajouté par `activate` y passait inaperçu. | (1) Déplacer/renommer un projet Python CASSE son venv : le reconstruire (`python3.12 -m venv .venv` par-dessus — réécrit les scripts, préserve site-packages) PUIS réécrire les shebangs, car `venv --upgrade` ne les touche pas. Contrôle en une ligne : `grep -rlF "<ancien_chemin>" .venv | wc -l` doit valoir 0. (2) Ne JAMAIS reproduire un bug d'environnement depuis un shell privilégié : neutraliser d'abord ce qui diffère (`env -u VIRTUAL_ENV PATH=/usr/bin:/bin ...`) — sinon on teste un cas qui n'est pas celui de l'utilisateur et on conclut « ça marche chez moi ». (3) Un indicateur d'état dérivé d'un NOM (prompt venv, badge, libellé) n'est pas une preuve de fonctionnement : vérifier la capacité réelle (`which python`, `command -v`), jamais l'affichage.
[2026-07-26] | La CI est passée au rouge sur `main` sans qu'une SEULE ligne de code ne change : 50 signalements ruff apparus d'un coup. Cause racine : le job Lint faisait `pip install ruff` sans version, et le dépôt ne déclarait AUCUNE configuration ruff — le jeu de règles appliqué était donc celui, implicite, de la version installée le jour même. Ruff 0.16.0 a élargi ses défauts ; mon ruff local (0.15.21) répondait toujours « All checks passed », d'où deux merges poussés sur une CI déjà cassée avant eux. J'ai d'abord soupçonné shellcheck (le job s'appelle « Lint (ruff + shellcheck) ») au lieu de lire `gh run view --log-failed`. | Un linter est une DÉPENDANCE, pas un outil ambiant : l'épingler dans `requirements-dev.txt` et faire installer la CI DEPUIS ce fichier (jamais un `pip install <outil>` nu dans le workflow), ET déclarer explicitement son jeu de règles (`[tool.ruff.lint] select = [...]`) — sinon le verdict change tout seul au gré des versions. Corollaire pour toute adoption de règles : les COMPTER avant de les activer (`ruff check --select X --statistics`) — ici `S` faisait 568 signalements (chaque `assert` de test) et `PTH` 94, à écarter ; et se méfier des règles pensées pour du code ASCII sur une base francophone (RUF001-003 signalaient le « × » d'une docstring comme un faux « x »). Enfin : lire le log du job AVANT de deviner lequel des deux outils a échoué.
[2026-07-27] | Vue Tableau : MAJ+clic sélectionnait n'importe quoi (« ça saute »). Cause : passer en Tableau ne DÉMONTE pas la vue Blocs, elle est seulement `hidden` — or `selectRange` faisait un `document.querySelectorAll(".person, .bt-row")` global. Chaque personne y figurait donc DEUX fois (carte masquée + rangée visible), et `indexOf` trouvait d'abord la carte, dans l'ordre des blocs et non celui du tri. Prouvé : sélection `[40, 20]` là où l'écran montre `[40, 30, 20]`. | Toute logique qui dépend de l'ORDRE VISUEL (plage MAJ+clic, navigation clavier, « premier/dernier ») doit interroger la SEULE vue active, jamais tout le document : un conteneur `hidden` reste dans le DOM et ses nœuds répondent encore aux sélecteurs. Corollaire : exclure aussi ce qui est estompé/`pointer-events:none` — balayer de l'invisible produit une sélection que l'utilisateur ne peut pas expliquer. Et un geste de sélection qui déplace un LOT doit le DIRE : le fantôme de drag par défaut ne montre que l'élément saisi, d'où `setDragImage` sur une pastille « N beltpacks ».
[2026-07-27] | Raccourci ⌘Z ajouté à l'admin : sans garde, il annulait TOUT le plateau alors que le curseur était dans un champ texte (on croyait corriger une faute de frappe, on effaçait le dernier groupe). Test de portée écrit d'abord en assertion NÉGATIVE (« le groupe est toujours là »), donc vert même sans la garde. | Tout raccourci qui EXISTE DÉJÀ nativement (⌘Z, ⌘A, ⌘C…) doit être neutralisé dans les champs de saisie et les dialogues — un seul prédicat partagé (`onBoard`), pas la condition recopiée à chaque branche. Et une assertion négative de ce type se valide en RETIRANT la garde : ici elle passait de `1 == 1` à `0 == 1`, preuve qu'elle mord. Pour la portée d'un « annuler », préférer une frontière STRUCTURELLE (l'historique ne connaît que `state.data`, la config boîtier vit dans d'autres endpoints) à une liste d'exclusions, qui se périme au premier réglage ajouté.
[2026-07-27] | Deuxième occurrence du faux négatif de grep filtré par le hook rtk : `grep -c 'if (false) selectTab'` a répondu « 0 matches » alors que la substitution perl avait bien eu lieu (ligne 1976). J'ai failli conclure que mon `perl -0pi` avait échoué. Déjà vu le 2026-07-07. | Pour toute vérification qui DÉCIDE de la suite (le motif est-il bien en place ? le fichier est-il restauré ?), utiliser `rtk proxy grep …` — le proxy court-circuite le filtrage. Règle générale inchangée : un résultat NÉGATIF d'un outil potentiellement filtré ne prouve rien, il se recoupe par une seconde méthode.
[2026-07-27] | `wait_for_selector("#selection-bar:not(.active)")` a expiré alors que la condition était remplie : la barre inactive est `display:none`, or Playwright attend `visible` par défaut. Le log (« resolved to hidden ») ne le dit pas franchement. Exactement la leçon du 2026-07-23, re-commise. | Attendre la DISPARITION d'un état (classe retirée, panneau replié) demande TOUJOURS `state="attached"` ou `state="hidden"`. Réflexe : dès qu'un `wait_for_selector` porte sur un `:not(...)` ou sur un conteneur potentiellement vide, préciser l'état explicitement.
[2026-07-28] | Retour Nathan : « Journal, Écran, Santé font très AI slop ». Le diagnostic n'était pas chromatique — c'était STRUCTUREL : six cartes arrondies identiques sur Santé, où « Cœurs : 8 » pesait autant que « carte SD pleine à 90 % » (la seule ligne capable de faire tomber un show), et une page nommée « Santé » qui ne disait jamais si le boîtier allait bien ; un Journal qui n'encodait pas le temps (date complète réimprimée à chaque ligne au lieu de servir d'axe). | La grille de tuiles équivalentes est le réflexe par défaut, et elle SUPPRIME la hiérarchie : elle affirme que tout compte pareil, ce qui est presque toujours faux. Avant de dessiner un écran d'état, écrire la QUESTION à laquelle il répond (« puis-je lancer le show ? ») et y répondre en premier, en clair ; les mesures ne sont que les preuves, triées par gravité, et une réglette ne se justifie que là où un SEUIL existe. Corollaire d'écriture : nommer par ce que ça fait pour l'utilisateur (« contrôle avant show ») et non par la technique (« monitoring de l'appliance ») ; et accorder les pluriels générés (« 1 groupes » trahit la machine plus sûrement qu'une faute de goût).
[2026-07-28] | Un `heredoc` Python (`.venv/bin/python - <<'PY'`) est revenu corrompu : `p.read_text(...)` exécuté en `p.read_text1(...)` → AttributeError. Le script n'a donc PAS tourné, mais le pytest qui suivait est passé — j'ai failli conclure « le test est vert, la vérification négative est faite », alors qu'elle n'avait jamais eu lieu. | Ne jamais faire dépendre une VÉRIFICATION d'un heredoc dans cet environnement (le hook rtk peut altérer le corps) : écrire le script dans un fichier du scratchpad et l'exécuter. Et enchaîner un test derrière une commande de préparation sans vérifier que la préparation a RÉUSSI donne un vert qui ne prouve rien — même famille de piège que le `pytest | tail` du 2026-07-26.
[2026-07-28] | Contrôle de code mort après refonte : `grep -c "h-card"` renvoyait 5 occurrences, laissant croire à des règles CSS orphelines. C'étaient `auth-card`, `dash-card` et `health-cards` — le motif capturait des sous-chaînes d'autres classes. | Pour chercher un SÉLECTEUR CSS, ancrer le motif (`\.h-card\b`, ou grep -n puis lecture) : un nom court est presque toujours sous-chaîne d'un autre, et le faux positif fait perdre autant de temps que le faux négatif. Corollaire de nommage relevé au passage : après refonte, un identifiant qui décrit l'ANCIENNE forme (`#health-cards` ne contenant plus de cartes) doit être renommé dans le même commit, sinon il ment au prochain lecteur.
[2026-07-28] | Le volet « Technique » du journal était rempli de lignes `werkzeug` — le journal d'ACCÈS HTTP. Cause racine : le tampon accroche le logger RACINE, il captait donc une ligne par requête, fichiers statiques compris ; 53 lignes après un seul chargement de page, sur un tampon de 500. L'outil censé permettre de diagnostiquer sans SSH était noyé par du bruit, et personne ne l'avait vu parce qu'on ne le regarde qu'en cas de problème. | Un tampon de logs branché sur la RACINE capte aussi les bibliothèques : énumérer ce que les dépendances journalisent AVANT de brancher, et écarter les journaux d'accès (`werkzeug`, `gunicorn.access`) — mais seulement EN DESSOUS de WARNING, sinon on perd les vraies erreurs du serveur. Corollaire d'affichage : un nom de logger Python est une adresse dans le code, pas une information pour l'utilisateur — afficher un nom de composant et garder le nom complet en infobulle.
[2026-07-28] | J'avais dessiné la conduite du journal avec un filet vertical continu ponctué d'un cran par événement. C'était joli et FAUX : les lignes sont espacées uniformément quel que soit le temps écoulé entre elles, donc le filet affirmait un axe proportionnel qui n'existe pas. Nathan l'a résumé par « pas belle ». | Un ornement qui ressemble à une échelle DOIT en être une. Avant de dessiner un axe, une jauge ou une frise, vérifier que la géométrie encode réellement la grandeur : sinon supprimer l'élément (ici les ruptures de journée portaient déjà la structure temporelle, et elles, elles sont vraies). Même principe que pour les réglettes de la page Santé : n'en poser que là où un SEUIL existe.
[2026-07-28] | Trois valeurs affichées en français avec un point décimal (« 408.1 Go », « 4.37 tâches ») parce que `toFixed()` est insensible à la locale, dans une base de code par ailleurs entièrement francophone. | `toFixed()` et la concaténation de nombres produisent TOUJOURS du format anglais : passer par `toLocaleString("fr-FR", {minimumFractionDigits})` dès qu'un nombre décimal est destiné à l'écran. À vérifier en même temps que les accords de pluriel — ce sont les deux fautes qui trahissent une interface générée.
[2026-07-28] | Audit complet : la page d'administration s'abonnait à `/events` comme un écran de régie. `broker.subscribe()` ne distinguant pas ses abonnés, ouvrir l'admin sans le moindre écran branché affichait « 1 afficheur » — dans la barre d'état ET dans la ligne de vie de la page Santé, celle qui répond à « puis-je lancer le show ? ». Deux tests existants « couvraient » le compteur, mais ils s'abonnaient DIRECTEMENT sur l'objet Broker, jamais par une requête HTTP : ils ne pouvaient pas voir le cas. | Un compteur qui va à l'écran doit compter ce que l'utilisateur CROIT qu'il compte : typer les abonnés (`subscribe(kind)`), et exposer deux mesures quand il y a deux questions (le TOTAL borne le pool de threads, les ÉCRANS vont à l'affichage). Corollaire de test : s'abonner via l'objet interne contourne précisément la couche qui contient le bug — au moins un test doit passer par le vrai chemin HTTP. Corollaire produit : un compteur alimenté par un changement DISTANT (on branche un écran ailleurs) n'a aucune action locale après laquelle se rafraîchir — c'est au serveur de l'annoncer (évènement `displays`), sinon il reste figé sur la valeur du chargement.
[2026-07-28] | `importConfig` reconstruisait le brouillon en énumérant ses champs à la main. Deux champs ajoutés après coup — `production_name` et `text_scale` — n'y avaient jamais été reportés : importer un fichier exporté la minute d'avant les effaçait en silence. Le piège avait DÉJÀ été relevé pour `skin` (« ⚠️ sinon perdu » dans todo.md) et il a resservi deux fois. | Tant qu'un humain doit « penser à » recopier un champ dans un second endroit, il l'oubliera — un commentaire d'avertissement ne fait que dater l'oubli. Le remède est structurel : UNE liste de champs (`DRAFT_FIELDS`), un filtrage générique, et un test qui confronte cette liste au modèle serveur (`empty_state()`), de sorte qu'ajouter un champ d'un côté fasse échouer la suite. Règle générale : remplacer l'énumération manuelle par une allowlist partagée dès qu'elle existe en deux exemplaires.
[2026-07-28] | Quatre fichiers d'état (`journal.jsonl`, `lifetime.json`, `network.json`, `viewer.json`) n'étaient pas dans `.gitignore` — `lifetime.json` traînait déjà en `??` à la racine, et `network.json` contient le PSK Wi-Fi EN CLAIR. Le README affirmait pourtant « Tous listés dans `.gitignore` ». | Une liste tenue à la main dérive dès le fichier suivant. La garder par un test qui interroge les SERVICES (`storage.draft_path`, `journal.path`…) au lieu de recopier des noms, et qui délègue le verdict à `git check-ignore` — réimplémenter la sémantique de .gitignore reviendrait à tester sa propre copie. Rappel : DATA_DIR vaut le répertoire COURANT par défaut, donc ces fichiers atterrissent à la racine du dépôt.
[2026-07-28] | Un `pytest.ini` coexistait avec `[tool.pytest.ini_options]` dans `pyproject.toml`. pytest.ini l'emporte TOUJOURS : la section du pyproject n'était jamais lue. Rien ne cassait (valeurs identiques), mais toute modification faite là serait restée sans effet, en silence. | Deux fichiers de configuration pour le même outil = un piège dormant, même quand ils s'accordent. Une seule source, et un commentaire qui dit pourquoi l'autre n'existe pas. Même famille que les allowlists dupliquées : ce n'est pas la divergence actuelle qui coûte, c'est celle qui viendra.
[2026-07-28] | `shellcheck` était encore installé par `apt-get install` (sans version) sur `ubuntu-latest`, alors que la panne de `main` du 2026-07-26 avait été causée EXACTEMENT par ce motif avec ruff — et corrigée pour ruff seul. Local 0.11.0, CI 0.9.0. | Quand une cause racine est identifiée, la corriger sur TOUS les occurrents, pas seulement celui qui a fait mal : lister les outils de la CI et vérifier que chacun est épinglé, image comprise (`ubuntu-24.04`, pas `ubuntu-latest`). Corollaire : fournir un moyen de rejouer le lint EN LOCAL avec les versions de la CI (`deploy/lint-local.sh` via Docker), sinon « vert chez moi » ne veut rien dire.
[2026-07-28] | J'ai écrit du CSS avec `var(--a-line)`, `var(--a-surface-2)`, `var(--a-surface-3)` — trois jetons qui n'existent pas (ils s'appellent `--border`, `--surface-2`, `--surface-3`). Aucune erreur : une variable CSS non définie retombe en SILENCE. Exactement la leçon du 2026-07-25, re-commise en l'écrivant. | Un contrôle textuel suffit et coûte trois lignes : extraire les `var(--x)` et les `--x:` de chaque groupe de feuilles chargées ensemble, et exiger l'inclusion. Fait ici (`test_css_tokens.py`), avec les jetons posés en CSSOM listés explicitement. À faire une fois pour toutes plutôt que de se relire.
[2026-07-28] | Un enregistrement différé (debounce 900 ms) parti avec l'ANCIEN brouillon revenait APRÈS une restauration de sauvegarde et réassignait `state.data` : le contenu restauré disparaissait de l'écran ET du serveur, sans message. Trouvé parce qu'un e2e échouait et que le journal réseau montrait un `PUT /api/draft` après le `POST /api/backup/restore`. | Une écriture atomique ne dit rien de l'ORDRE de deux écritures (corollaire client de la leçon serveur du 2026-07-06). Tout remplacement EN BLOC (restauration, import, chargement de configuration, resynchro distante) doit invalider les enregistrements en vol : un compteur de génération incrémenté par la reprise, comparé au retour de la requête, et la réponse périmée est jetée. Corollaire produit : toute action qui lit l'état CÔTÉ SERVEUR (sauvegarde, aperçu, publication) doit d'abord vider la file d'enregistrement — sinon elle travaille sur un état d'avant, en silence.
[2026-07-28] | Instrumentation e2e posée en `add_init_script` : la première ligne installait un `MutationObserver` sur `document.documentElement`, qui n'existe pas encore à cet instant. L'exception a emporté EN SILENCE tout le reste du script — dont le journal des évènements SSE. Le symptôme était trompeur (`window.__sse` undefined alors que `window.__anim`, défini juste avant, existait). Et le vrai danger n'était pas là : si l'observateur avait échoué APRÈS avoir posé son tableau, mon assertion `__anim == []` (« aucune animation sur reconnexion ») serait passée au vert sans que rien n'ait jamais été observé. | Un script d'instrumentation injecté avant la page est du code non testé qui porte des assertions NÉGATIVES : (1) n'y rien faire qui puisse lever avant d'avoir posé tous les globals — observer `document`, jamais `document.documentElement`, qui peut être nul au démarrage ; (2) toute assertion « il ne s'est rien passé » doit s'appuyer sur un témoin POSITIF prouvant que la sonde a fonctionné (ici `__sse == ["snapshot"]` : le flux a bien été reçu, donc l'absence d'animation est une vraie information). Même famille que le collecteur console creux du 2026-07-23.
[2026-07-28] | Deux vrais défauts trouvés par les outils, pas par la relecture : ruff a signalé `assert p["configs"] if "configs" in p else True` (SIM401), assertion vraie même quand la clé MANQUAIT — et elle manquait, `build_payload` collectait les configurations sans jamais les renvoyer ; et un test de port a révélé que la sonde de découverte lisait la constante pour le `connect` TCP mais gardait 80 en dur dans l'URL. | Une assertion CONDITIONNELLE ne prouve rien : `assert x if k in d else True` est du bruit qui ressemble à un test. Écrire l'assertion ferme, quitte à la voir échouer tout de suite — c'est le but. Et un paramètre qui n'est honoré que par la moitié d'une fonction est invisible tant qu'on ne l'exerce pas avec une valeur non nulle : tester un réglage avec une valeur DIFFÉRENTE du défaut, jamais avec le défaut.
[2026-07-29] | `deploy/set-branding.sh` ancrait son `sed -i` sur `Environment=DATA_DIR=`, une ligne qui n'existe que dans `deploy/comroster.service` — le MODÈLE versionné. L'unité réellement installée est générée par `setup-pi.sh` et utilise `EnvironmentFile=/etc/comroster.env`. Un `sed` sans correspondance renvoie 0 : le script affichait « ✅ Marque posée » alors que la variable n'était jamais déclarée. | Deux règles. (1) Écrire pour le fichier RÉELLEMENT généré sur le boîtier, jamais pour le modèle du dépôt — `deploy/comroster.service` porte d'ailleurs l'avertissement « CE N'EST PAS CE QUI TOURNE ». (2) Un `sed -i` d'insertion ne signale pas l'absence de sa cible : vérifier explicitement que le motif existe, ou refuser.
[2026-07-29] | Le même script validait les extensions en minuscules (`.lower()`) mais copiait via `find -name '*.svg'`, sensible à la casse. Un pack contenant `LOGO.SVG` était déclaré valide, annoncé posé, et l'application retombait en silence sur ComRoster faute de trouver le fichier. | Quand un nom est validé puis utilisé, se servir DU nom validé — jamais d'un motif re-dérivé qui rejuge la même chose avec d'autres règles. Deux jugements sur la même donnée finissent toujours par diverger.
[2026-07-29] | Chargeur fail-safe (`Branding`) : `(manifeste.get("name") or "").strip()` sur une valeur de JSON non vérifiée. Un `{"name": 123}` levait un `AttributeError`, que le `except (OSError, ValueError)` ne rattrapait pas — le boîtier aurait planté au démarrage à cause d'une faute de frappe dans un manifeste. | Dans un chargeur qui promet de ne jamais lever, vérifier le TYPE avant d'appeler une méthode de type. Le `or ""` protège du `None`, pas du `int`. Et tester au moins un champ mal typé, pas seulement des champs absents.
[2026-07-29] | Le kiosk transmet la version au splash via une chaîne de requête (`boot-splash.html?v=…`) : sans encodage explicite, « v1.4.0+7 » serait devenu « v1.4.0 7 » à l'écran de démarrage — dans une query string, « + » se décode en ESPACE, pas en lui-même. Un piège qu'aucun test unitaire ni relecture de code n'attrape à l'œil : seul le rendu réel le montre. | Tout caractère « + » (ou autre caractère réservé RFC 3986) injecté dans une chaîne de requête doit être percent-encodé (`%2B`) au moment de la construction de l'URL, jamais laissé tel quel en espérant que le récepteur le retolère. Corollaire de vérification : pour un défaut qui ne se voit qu'à l'écran, un test source qui grep le motif d'encodage (`assert "%2B" in source`) est nécessaire mais ne remplace pas un contrôle visuel du rendu réel avant de clore la tâche.
[2026-07-29] | `Journal.record()` écrivait sans protection contre les erreurs disque. Tant que l'appel restait dans un gestionnaire de requête HTTP, une `OSError` n'aurait cassé qu'UNE requête. Le câbler dans `create_app()` pour journaliser l'événement `startup` (avec la version) a transformé ce même défaut dormant en défaut capable d'empêcher le boîtier de démarrer — sans qu'une seule ligne de `record()` n'ait changé au moment du déplacement. | Déplacer un appel EXISTANT vers le chemin de démarrage (`create_app()`, une unité systemd, un script d'installation) change sa CRITICITÉ même quand son code reste identique : avant tout déplacement de ce genre, relire l'appelé et vérifier qu'il est fail-safe (E/S sous try/except large, jamais d'exception qui remonte) — pas après un incident. Corollaire : `create_app()` ne doit contenir que des opérations dont l'échec est tolérable, sinon le boîtier entier ne démarre plus.
[2026-07-30] | Le correctif fail-safe de la tâche 1 (`except (OSError, ValueError)`, parce que `UnicodeDecodeError` dérive de ValueError et NON d'OSError) a été appliqué à `_charger()`, puis jamais reporté sur `_premiere_ligne()` et `_reference_compactee()` — deux lecteurs de fichiers ajoutés par la tâche 2, dans le MÊME module, pendant le MÊME lot. Un octet corrompu dans `.git/HEAD` faisait donc lever `create_app()` : gunicorn boucle sur `Restart=on-failure` toutes les 3 s et l'écran de régie ne revient JAMAIS. Neuf relectures par tâche ne l'ont pas vu — chacune ne regardait que son propre diff, le défaut naissait de la juxtaposition. Seule la revue de la branche entière l'a trouvé, en remarquant l'asymétrie des tests : la corruption du fichier VERSION avait son test dédié, celle des fichiers `.git` non. Récidive directe de la leçon du 2026-07-28 sur shellcheck. | Corriger une cause racine, c'est corriger sa CLASSE : après tout correctif, chercher les autres sites du même motif (`grep -n 'except OSError' ` sur le module puis sur le paquet) et les traiter dans le même commit — y compris les sites qui n'existent pas encore, en portant la règle dans les consignes des tâches suivantes. Corollaire de processus : une relecture par tâche est structurellement aveugle à un défaut né de la juxtaposition de deux tâches, donc une revue de la branche ENTIÈRE reste nécessaire même quand chaque tâche a été relue et approuvée. Meilleur indice à chercher dans cette revue : l'asymétrie de couverture — un cas de défaillance testé ici, le même cas non testé là.
[2026-07-30] | Test « Échap ferme le menu AVANT de quitter la sélection » : écrit pour garder un ORDRE de branches, il passait dans les DEUX ordres. Cause : j'avais aussi ajouté `!settingsOpen()` au prédicat partagé `onBoard`, qui protège déjà la sélection — le rang des deux branches était donc sans effet. Sans la mutation de contrôle, j'aurais livré un test qui affirme garder une décision qu'il ne garde pas, ET un commentaire de code qui répète la même chose fausse. Le seul rang qui décide réellement est celui face au décompte de publication, qui lui ne consulte pas `onBoard`. | Quand deux protections se recouvrent (un ordre d'exécution ET un prédicat de garde), la mutation ne suffit pas : il faut identifier LAQUELLE porte le comportement. Avant d'écrire un test d'ordre, se demander « que se passe-t-il si j'inverse ? » — si la réponse est « rien », l'ordre n'est pas la propriété à tester, et le commentaire qui l'invoque est déjà faux. Corollaire : chercher le rang qui compte du côté de ce qui NE passe PAS par le prédicat commun.
[2026-07-30] | Trois faux signaux en une exécution, tous du même genre : (1) `pytest tests/e2e -q` a répondu « 30 deselected » — le marqueur `e2e` est exclu par défaut, `-m e2e` est obligatoire, et le code retour 0 ressemblait à un succès ; (2) `from .helpers import` a échoué parce que `tests/e2e` n'est pas un package (aucun `__init__.py`) ; (3) une assertion `"Lumière" in inner_text(...)` a échoué alors que la garde testée fonctionnait — le CSS met les noms de groupe en capitales, `inner_text` renvoyait « LUMIÈRE ». | Un test « vert » se lit sur le NOMBRE de tests exécutés, jamais sur le code retour seul : `0 passed` et `N deselected` sont des échecs de vérification déguisés. Et toute assertion sur du texte d'interface doit être insensible à ce que le CSS transforme (`text-transform`, espaces) : comparer en `.lower()`, ou viser la donnée plutôt que le rendu.
[2026-07-30] | J'allais ÉCARTER le numéro de page de la feuille imprimée en affirmant que Chromium ne gère pas les boîtes de marge CSS (`@page { @bottom-right { content: counter(page) } }`). C'était vrai il y a quelques versions, ça ne l'est plus : une sonde de trente lignes (PDF réel relu par `pdftotext`) a répondu « page 1 / 2 » puis « page 2 / 2 ». Deux autres capacités que je m'apprêtais à contourner marchaient aussi — `position: fixed` répété par page, et `insertRule('@page { size: … }')` à chaud, qui a rendu inutile tout le mécanisme de rechargement que j'avais prévu. | Une capacité de PLATEFORME (navigateur, noyau, pilote) ne se refuse jamais sur une croyance : elle se sonde, et la sonde coûte moins cher que le contournement qu'on écrirait sinon. Écarter une fonction sur une connaissance périmée est le pire des faux négatifs — personne ne la réclamera plus jamais, et le contournement, lui, restera dans le code. Corollaire : annoncer honnêtement « je croyais X impossible, la mesure dit Y » plutôt que de livrer le contournement en silence.
[2026-07-30] | Format papier jugé sur une capture de 27 beltpacks : mise en page validée, défaut invisible. Sur un plateau RÉALISTE (62 beltpacks, 9 groupes) le vrai défaut a sauté — un groupe de plus de douze membres se coupait entre deux colonnes SANS se réidentifier, la suite n'étant qu'une liste de numéros orpheline. Défaut que j'avais moi-même introduit en rendant les groupes longs coupables, et dont le commentaire du code décrivait exactement le danger (« lire la moitié d'un groupe au verso est ce qui fait rater une affectation »). Aucun test sur le DOM ne pouvait le voir : le balisage était correct, c'est la RÉPÉTITION à l'impression qui manquait. | Un rendu ne se juge que sur un jeu de données REPRÉSENTATIF de l'usage réel : un échantillon trop petit n'active pas les seuils, donc pas les branches où vivent les défauts. Avant de valider une mise en page, se demander « quel seuil mon jeu de test n'atteint pas ? » et le franchir exprès. Corollaire pour l'impression : ce qui se répète (en-tête de tableau, bandeau, numéro) ne se vérifie que sur un document à PLUSIEURS pages ou colonnes, et se lit dans le PDF, pas dans le DOM — `pdftotext` compte les occurrences, l'œil non.
[2026-07-30] | `perl -0pi -e` pour substituer du texte dans un template : le remplacement contenait un `\x{2610}` (☐). Ce seul caractère large fait basculer le scalaire en UTF-8 interne, et Perl ré-encode alors TOUT le fichier à l'écriture — les octets déjà UTF-8 sont doublement encodés, et l'intégralité du texte préexistant est partie en « Ã© » et « Â· ». Le seul signe était un discret « Wide character in print », et mes propres lignes insérées, elles, s'affichaient correctement — ce qui rendait le dégât facile à manquer. | Ne pas utiliser `perl -0pi` sur un fichier UTF-8 dès que la substitution contient un caractère non-ASCII : passer `-CSD`, ou utiliser l'outil d'édition. Et après toute réécriture programmatique d'un fichier accentué, CONTRÔLER l'encodage (`grep -c "Ã©\|Â·"` doit valoir 0) — un fichier à moitié mojibaké passe les tests tant qu'aucune assertion ne porte sur les accents.
[2026-07-30] | Fusionner trois documents en panneaux d'un seul : les défauts ne sont pas là où je regardais. Le plus grave n'était PAS dans le nouveau code mais dans l'ancien — `admin.js` liait la bascule Blocs/Table à `document.querySelectorAll(".tb-seg .seg-btn")`, sélecteur correct tant que la barre du plateau était la seule à porter ces classes. Le panneau Journal en apporte deux (Événements/Technique) : cliquer « Technique » aurait appelé `setViewMode(undefined)` et fait disparaître LES DEUX vues du plateau, depuis un autre panneau. Rien dans le diff du panneau ne l'aurait montré. Deuxième défaut de la même famille : `status-info` existait à l'identique dans `journal.html` et `health.html` — réunis, l'un des deux devenait introuvable. | Avant de fusionner des documents, inventorier les COLLISIONS de l'espace de noms partagé, dans les deux sens : (1) tout `document.querySelector*` par CLASSE devient ambigu dès qu'un second porteur entre dans la page — le borner à son conteneur AVANT d'ajouter le nouveau contenu, sinon on introduit soi-même le défaut ; (2) les identifiants des documents fusionnés doivent être diffés entre eux, pas seulement relus. Garde durable : un test qui refuse tout `id` en double dans le document fusionné, et un test qui confronte chaque `getElementById` des scripts au balisage réellement présent — ces scripts avaient leur propre page pour garantir leur décor, ils ne l'ont plus.
[2026-07-30] | Un sondage périodique (`setInterval` + `!document.hidden`) était juste tant que sa page ne montrait QUE lui : Journal battait toutes les 5 s, Santé toutes les 4 s. Devenus panneaux, ces mêmes boucles interrogeaient le boîtier en permanence pendant qu'on travaille sur le plateau — trois requêtes par battement, sur un Raspberry Pi, pour des vues que personne ne regarde. Le code n'avait pas changé d'une ligne ; c'est son CONTEXTE qui a changé. | Déplacer du code d'un document dédié vers un document partagé change les hypothèses qu'il avait le droit de faire — « ma page est visible donc on me regarde » devient faux. Refaire l'inventaire de ces hypothèses implicites (visibilité, unicité, cycle de vie) avant de déplacer, comme on le fait déjà pour la criticité quand on déplace un appel vers le démarrage (leçon 2026-07-29). Et le vérifier en COMPTANT les requêtes réelles dans le navigateur, pas en relisant la condition : c'est le trafic qui coûte, donc c'est lui qu'il faut mesurer.
[2026-07-30] | Le doublon d'affichage du panneau Journal — le compteur disait « 2 événements » et, deux centimètres à droite, la phrase d'état redisait « 2 événements · 0 ligne technique en mémoire » — n'a été vu QUE sur la capture d'écran. Il avait traversé la conception, l'écriture, 517 tests unitaires et 53 e2e, tous verts : chaque élément était présent, correct, et testé pour ce qu'il valait individuellement. | Deux éléments justes côte à côte peuvent former un défaut que ni l'un ni l'autre ne porte, et aucune assertion ne le voit — une redondance est une propriété de la COMPOSITION. Quand on rapatrie dans un cadre des éléments qui vivaient chacun dans le leur (un chip d'en-tête, une phrase de pied), rendre l'écran et le regarder fait partie de la vérification, pas du confort. Corollaire : le premier réflexe face à deux témoins voisins est de demander « lequel des deux répond à une question qu'on se pose vraiment ? » et de supprimer l'autre, en récupérant ce qu'il portait d'irremplaçable.
[2026-07-31] | `pytest -q` s'interrompait à la COLLECTE : « import file mismatch » entre `tests/test_panneaux.py` et `tests/e2e/test_panneaux.py`. Deux fichiers de test homonymes dans un arbre sans `__init__.py`, où `tests/e2e` entre dans `sys.path` (les e2e s'importent `from helpers import …`) : le second module écrase le premier. Le défaut était DORMANT — des `__pycache__` compatibles le masquaient — et n'est apparu qu'au premier nettoyage de cache, donc bien après le lot qui l'avait introduit. Une suite entière bloquée, pour un nom de fichier. | Dans une arborescence de tests sans `__init__.py`, le nom de fichier est un IDENTIFIANT GLOBAL : avant de créer `tests/<zone>/test_x.py`, vérifier qu'aucun `test_x.py` n'existe ailleurs (`find tests -name 'test_*.py' -exec basename {} \; | sort | uniq -d`). Corollaire de vérification : un `pytest` vert ne prouve rien tant que les `__pycache__` n'ont pas été purgés au moins une fois — un cache périmé masque exactement les défauts d'IMPORT, ceux qui bloquent tout.
[2026-07-31] | Un e2e enregistrait une configuration juste après avoir ajouté un beltpack : le fichier écrit ne contenait RIEN. `POST /api/configs` fait relire le brouillon PAR LE SERVEUR, or l'admin l'écrit avec un debounce — le DOM était à jour, le disque non. Le test échouait sur une assertion de contenu (`'Régie' in []`), à trois écrans de la vraie cause. Le helper qui attend ce cycle existait, mais privé dans `test_e2e.py` sous le nom `_wait_saved`. | Dès qu'un test déclenche une action dont le SERVEUR relit un état que le navigateur vient de modifier (enregistrer une config, publier, sauvegarder), attendre explicitement la fin du cycle d'écriture — jamais se fier à la présence de l'élément dans le DOM, qui est toujours en avance. Corollaire : un helper d'attente privé à un fichier de tests est un piège à récidive ; il appartient à `helpers.py` dès son deuxième besoin.
[2026-07-31] | En rédigeant un docstring français j'ai écrit « воisines » — deux caractères CYRILLIQUES homoglyphes (в, о) au milieu d'un mot latin. Invisible à la relecture, invisible aux tests (aucune assertion ne porte sur un commentaire), et ruff ne s'en émeut pas. Rattrapé par hasard, en relisant mon propre diff avant de retenter une édition bloquée. | Après toute rédaction de prose accentuée (docstring, commentaire, libellé d'interface), passer un contrôle d'alphabet sur le diff : `git diff -U0 | grep -nP '[^\x00-\x7F]' ` puis vérifier que chaque caractère non-ASCII est bien un accent latin attendu (`python3 -c "import unicodedata,sys; [print(c, unicodedata.name(c)) for c in set(sys.stdin.read()) if ord(c)>127]"`). Les homoglyphes cyrilliques/grecs traversent relecture, tests et lint sans laisser de trace.
[2026-07-31] | Première mutation de contrôle sur l'export d'une config : j'avais faussé À LA FOIS la source du contenu ET le slug. Le test est tombé — mais sur le NOM du fichier (`comroster-mutant.rost`), sans jamais atteindre l'assertion de contenu. La garde que je croyais éprouver (« le fichier vient du disque, pas de l'écran ») n'avait rien prouvé. | Une mutation doit fausser UNE SEULE propriété : si elle en casse deux, c'est la première assertion rencontrée qui parle et les suivantes ne sont pas exercées. Quand un test porte plusieurs assertions indépendantes, muter une par une — et vérifier que le message d'échec nomme bien la propriété visée, pas une autre.
[2026-08-03] | Mutation de contrôle sur « ignorer `updated_at` dans la comparaison brouillon/publié » : elle est PASSÉE au vert, donc mon test ne prouvait rien. Deux causes empilées. (1) `now_iso()` a une granularité d'une SECONDE : un test qui publie puis modifie en quelques millisecondes réécrit exactement le même horodatage, si bien que la comparaison brute et la comparaison filtrée rendaient le même verdict. (2) En cherchant pourquoi, un défaut du lot précédent est apparu : `add_group()` construisait le groupe à la main sans `manual_order`, que `build_draft()` ajoute — deux formes de groupe selon le chemin de création, et un faux écart entre brouillon et publié dès qu'un enregistrement complet normalisait l'un des deux. | Quand une mutation ne fait pas tomber son test, ne pas se contenter d'ajuster le test : chercher POURQUOI le comportement muté est indiscernable. La réponse est souvent une grandeur trop grossière pour l'échelle du test (horodatage à la seconde, cache, debounce) — la FORCER (`monkeypatch` sur l'horloge) plutôt que dormir, et ajouter un témoin positif prouvant qu'elle a réellement changé. Corollaire trouvé au passage : dès qu'un champ est ajouté au normaliseur d'un modèle, vérifier les CONSTRUCTEURS directs (`add_*`, `new_*`) qui fabriquent la même entité sans passer par lui — sinon deux formes de la même chose coexistent, et cela ne se révèle que par une comparaison d'états, jamais par un test de fonctionnalité.
[2026-08-03] | Nathan a donné une liste de treize retours d'usage ; le travail a commencé et deux commits en portent une bonne part — mais la LISTE n'a jamais été écrite dans tasks/todo.md. Je lui ai ensuite dit de faire un `/clear` en affirmant que je reprendrais « comme si de rien » grâce à todo.md et lessons.md. La session suivante a repris sur un todo.md qui n'en portait pas un mot : il a fallu que Nathan retape ses treize points, et j'avais entre-temps travaillé sur de tout autres restes. | Une demande de l'utilisateur s'écrit dans `tasks/todo.md` AVANT la première ligne de code, jamais en fin de lot : le fichier de suivi n'est pas un compte rendu, c'est le seul état qui survit à un `/clear`. Corollaire : ne jamais proposer un `/clear` (ni annoncer une reprise « sans perte ») sans avoir VÉRIFIÉ, en relisant le fichier, que la demande en cours y figure intégralement — les commits déjà faits ne la portent pas, ils portent leurs réponses, ce qui est exactement ce qu'on ne peut pas reconstituer.
[2026-08-03] | `display.js` écrivait cinq messages d'état dans deux éléments retirés du template TROIS SEMAINES plus tôt (`#sync-hint`, `#admin-hint`, commit 0156f0d), chacun protégé par un `if (el)` — donc aucune erreur, aucun symptôme, et « Mises à jour en direct actives » ou « ⚙ Admin : comroster.local » ne s'affichaient nulle part. Le premier avait été repéré et laissé en note « à trancher » ; le second n'avait jamais été vu. La garde qui les aurait attrapés existait déjà — mais pour `journal.js`/`health.js` face à `admin.html` seulement, écrite à la fusion des panneaux. | Le `if (el)` qui rend un script « robuste » à l'absence d'un élément rend AUSSI sa disparition indétectable : il transforme une erreur en silence. Toute page servie par un template mérite la même garde structurelle — chaque `getElementById` du script confronté à la page RENDUE — et cette garde s'écrit pour TOUS les couples script/template le jour où on l'écrit pour un seul (récidive de la leçon 2026-07-28 sur shellcheck : corriger une cause racine, c'est corriger sa classe). Corollaire de tri : avant de supprimer un message devenu invisible, chercher lequel portait une information que rien d'autre ne dit — ici quatre doublaient le voyant, le cinquième (« pas d'EventSource ») était le seul cas où l'écran se figeait en affirmant « En direct ».
[2026-08-02] | Le bouton « Supprimer » de la liste des configurations portait `class="chip-btn danger"` et s'affichait GRIS, identique à « Charger » et « Exporter ». Les deux classes étaient inertes à cet endroit : les seules règles qui les définissent exigent un ancêtre (`.admin-dialog .dialog-actions .danger`, `.block-actions .chip-btn`), absent de la liste. Rien ne plantait, aucun test ne pouvait le voir — un bouton gris est parfaitement fonctionnel — et le balisage, lui, affirmait le contraire à quiconque le relisait. | Une classe posée dans le balisage n'est PAS une preuve d'apparence : ce qui décide, c'est l'existence d'une règle dont le contexte est satisfait. Après avoir écrit ou déplacé un élément portant une classe sémantique (`danger`, `primary`, `muted`), vérifier qu'elle produit quelque chose — `getComputedStyle` sur l'élément RÉEL, comparé à celui d'un voisin censé différer. Corollaire de conception : quand on élargit un sélecteur à un nouveau contexte, séparer d'abord ce qui relève de l'APPARENCE (transposable) de ce qui relève du PLACEMENT (`margin-right: auto`, positionnement) — le second est presque toujours propre au contexte d'origine et casse silencieusement l'alignement du nouveau.
[2026-08-04] | Nathan : « tout est trop éloigné du bouton envoyer, il y a un creux moche ». J'avais MESURÉ ce header en juillet et conclu « les quatre segments sont contigus, il n'y a aucun vide à supprimer » — mesure exacte, et réponse à côté de la question. Les boîtes se touchaient bien (0 px entre elles), mais deux d'entre elles portaient une largeur figée sur leur pire cas et laissaient leur réserve en vide INTERNE : 69 px dans la chip d'état (figée pour « 88 en attente », affichant « À jour », contenu calé à gauche) et 48 px dans le bouton (figé pour « Annuler la publication · 5 », contenu centré). L'utilisateur voyait donc 117 px de trou entre deux MOTS, là où mes chiffres décrivaient des RECTANGLES jointifs. Sa formule « on dirait que c'est aligné vers la droite » nommait exactement le mécanisme, et je l'ai d'abord lue comme une impression vague. | Mesurer l'espace ENTRE les conteneurs ne dit rien de l'espace que l'œil perçoit : dès qu'un élément porte une largeur figée (`min-width` de réservation, colonne de grille, `flex-basis`), la seule métrique honnête est la distance entre les GLYPHES — récidive directe de la leçon du 2026-07-23 (aligner du texte se mesure à la ligne de base, pas au centre des boîtes). Corollaire de conception : une réserve de largeur est presque toujours nécessaire (sans elle l'interface saute), mais son CÔTÉ est un choix libre — la placer à l'opposé de l'action, jamais entre l'information et le bouton qu'on va cliquer. Corollaire de classe (leçon 2026-07-28) : l'arbitrage « ne pas réserver la place d'un état rare » avait été pris en juillet pour la chip d'état et jamais porté sur le bouton voisin, qui souffrait du même défaut dans le même en-tête. Et quand l'utilisateur décrit un défaut visuel que ma mesure dément, c'est ma MÉTRIQUE qu'il faut mettre en cause avant sa perception.
[2026-08-04] | Mutation de contrôle terminée, j'ai « annulé » avec `git checkout static/css/auth.css` et `git checkout tests/test_pages_et_scripts.py`. Les deux fichiers n'étaient pas commités : la commande ne m'a pas rendu l'état d'AVANT la mutation, elle m'a rendu celui de HEAD — c'est-à-dire l'ancienne feuille turquoise et la version du test sans mes ajouts. Tout le travail du lot sur ces deux fichiers a été effacé d'un coup, en silence (aucune erreur, aucun avertissement), et je ne l'ai vu que parce que le harnais m'a montré le contenu restauré. Le troisième fichier, lui, a bien résisté — mais pour la pire des raisons : `git checkout` a ÉCHOUÉ dessus, faute d'être connu de git. | `git checkout <fichier>` n'annule pas « la dernière modification », il écrase le fichier avec HEAD : sur du travail non commité, c'est une suppression définitive. Pour une mutation de contrôle, ne jamais s'en servir comme bouton d'annulation — sauvegarder d'abord (`cp` vers le scratchpad, ou `git add` pour que l'index serve de point de retour), et restaurer depuis CETTE copie. Corollaire : avant de lancer une série de mutations, vérifier lesquels des fichiers touchés sont suivis ET commités (`git status --short`), parce que le comportement de la commande de nettoyage change du tout au tout selon le cas — et le cas le plus dangereux est celui qui réussit sans rien dire.
[2026-08-04] | Première capture de la nouvelle page de connexion : le bouton « Se connecter » apparaissait en aplat rouge plein, alors que la feuille le veut neutre au repos et n'accentue qu'au survol. J'ai failli conclure à un défaut de cascade et retoucher le CSS. La vraie cause était le harnais : Playwright laisse le curseur là où le clic précédent l'a posé, et ce point SURVIT à la navigation — le parcours cliquait « Créer le compte » sur setup, puis chargeait /admin/login, dont le bouton occupe la même position. Je photographiais donc un `:hover`. | Une capture d'écran automatisée montre l'état de survol de ce qui se trouve sous le curseur, hérité de l'action précédente : déplacer la souris hors zone (`page.mouse.move(4, 4)`) avant CHAQUE prise, sinon on juge un état interactif en croyant voir le repos — et l'on « corrige » un style qui n'a rien. Même famille que le collecteur console creux : l'instrument fausse la mesure avant que la mesure ne parle du produit. Corollaire : quand un rendu contredit une règle CSS qu'on vient d'écrire, suspecter l'état (:hover, :focus, :active) avant la cascade — `autofocus` produit le même effet sur le premier champ, et c'est légitime, lui.
[2026-08-04] | J'ai renommé la classe du bouton d'authentification (`auth-submit` → `auth-go`) sans chercher qui la visait. HUIT fichiers e2e cliquent `a.auth-submit` pour se connecter : tous ont expiré au bout de 30 s, avant d'atteindre ce qu'ils testaient — 15 échecs qui n'avaient aucun rapport avec leur sujet. Le piège était DÉJÀ documenté ici : au lot « Réglages » (2026-07-30), sept clics e2e visaient des ids en direct et cela avait été repéré AVANT d'écrire le code. Je ne l'ai pas rejoué. | Renommer une classe ou un id, c'est modifier une INTERFACE que les tests consomment : chercher ses porteurs (`grep -rn "<ancien-nom>" tests/ static/ templates/`) AVANT de renommer, jamais après le premier rouge. Corollaire aggravant trouvé au passage : ces huit fichiers dupliquent chacun leur propre fonction de connexion, si bien qu'un renommage coûte huit corrections au lieu d'une — c'est exactement le motif que la leçon du 2026-07-31 condamne (« un helper privé à un fichier de tests appartient à helpers.py dès son deuxième besoin »), ici au huitième besoin. La duplication ne coûte rien tant qu'on ne touche à rien ; elle facture d'un coup le jour du renommage.
[2026-08-04] | Suite e2e lancée alors que la précédente tournait encore : 59 échecs en 27 minutes. Relancée SEULE, la même suite donne 62 verts en 101 secondes. Deux exécutions Playwright concurrentes se disputent le processeur et les navigateurs, et tout part en `TimeoutError` — des échecs qui accusent le code alors qu'ils ne parlent que du harnais. J'ai failli chercher la cause dans mes propres modifications. | Une suite de bout-en-bout ne se lance JAMAIS en parallèle d'elle-même : vérifier qu'aucune exécution n'est en cours (`ps aux | grep pytest`) avant d'en démarrer une, et ne pas empiler les relances « pour aller plus vite » quand la première tarde. Corollaire de lecture : un résultat massivement rouge dont un sous-ensemble passe au vert quand on le joue seul accuse l'ENVIRONNEMENT, pas le code — le vérifier en réexécutant isolément avant d'ouvrir le moindre fichier source. Corollaire de journal : un `exit code 0` accompagné d'un fichier de sortie vide ou tronqué ne prouve rien ; ne conclure que sur la ligne de synthèse (`N passed`), jamais sur le code retour seul (rappel du 2026-07-30).
[2026-08-05] | Refonte de la feuille imprimée : j'ai mis les noms de groupe en capitales via `text-transform`. Deux e2e qui lisent un VRAI PDF sont tombés sur `texte.count("Lumière") >= 2` — parce que Chromium RÉALISE la transformation typographique au rendu, si bien que `pdftotext` extrait « LUMIÈRE ». Les tests avaient raison de tomber, mais pour une raison sans rapport avec ce qu'ils gardent : la réidentification d'un groupe coupé, elle, fonctionnait parfaitement. J'ai failli chercher le défaut dans la pagination. | `text-transform` n'est pas un effet d'affichage réversible : ce qu'il produit descend dans le PDF, dans le presse-papiers et dans l'arbre d'accessibilité. Toute assertion portant sur du texte d'interface — et A FORTIORI sur un PDF extrait — se compare en casse normalisée (`.casefold()`), ou vise la donnée plutôt que le rendu. Récidive exacte de la leçon du 2026-07-30 (n°73), qui l'avait déjà établi pour `inner_text` : la règle valait pour TOUS les extracteurs de texte, pas seulement pour celui qui m'avait mordu ce jour-là — corriger une cause racine, c'est corriger sa classe (leçon du 2026-07-28).
[2026-08-05] | J'avais écrit noir sur blanc, en refondant la feuille imprimée, que « le fond du bandeau n'est JAMAIS la couleur du groupe : poser du texte sur une teinte saisie par l'utilisateur ne garantit aucun contraste ». Nathan a demandé l'inverse deux heures plus tard — et il avait raison, parce que le produit contenait DÉJÀ les deux garde-fous qui rendent la chose sûre : `inkFor` (luminance, seuil .179, partagée par l'écran et l'admin) et un nuancier borné à des teintes calibrées ≥ 4,5:1. Mon objection était juste dans l'absolu et fausse dans CE produit : j'ai raisonné sur « une couleur quelconque » alors que la couleur y est déjà contrainte. | Avant de refuser une direction visuelle au nom d'un risque (contraste, lisibilité, accessibilité), vérifier si le produit ne l'a pas déjà neutralisé ailleurs — une contrainte posée dans un lot précédent (palette bornée, règle d'encre partagée) change ce qui est permis dans le suivant, et l'oublier fait refuser au client quelque chose qu'on sait déjà faire. Corollaire de mise en œuvre : quand la règle existe, la RÉUTILISER (charger `ink.js`), jamais la réimplémenter dans le nouveau support — deux implémentations d'un même seuil divergent sans que rien ne le signale, puisqu'on ne compare jamais les deux rendus au même instant.
[2026-08-05] | Première feuille imprimée avec les couleurs « mises en avant » : tous les bandeaux sont sortis GRIS. J'ai failli chercher le défaut dans le CSS et dans le calcul d'encre. La cause était mon jeu d'essai — les groupes étaient créés par l'API sans champ `color`, donc le template retombait sur son défaut `#555555`. Le code était juste ; c'est l'échantillon qui n'exerçait pas du tout la propriété que je voulais juger. | Un jeu de données de test doit exercer LA propriété qu'on regarde, pas seulement produire un rendu plausible : avant de conclure quoi que ce soit d'une capture, vérifier que l'échantillon porte les valeurs concernées (ici des couleurs, prises dans la palette réelle du produit) et qu'il couvre les BRANCHES du code — deux teintes claires et deux sombres pour exercer les deux sorties d'une règle d'encre. Même famille que la leçon n°35 (juger une mise en page sur 27 beltpacks quand le seuil de coupure est à 12), mais sur les valeurs plutôt que sur le volume.
[2026-08-05] | Mode monochrome ajouté à la feuille imprimée : la case était cochée, l'attribut `data-mono="oui"` bien posé sur `<html>`, la règle CSS bien écrite — et le PDF sortait EN COULEUR. Ma règle neutralisait la variable (`[data-mono="oui"] .sheet-group-name { --gel: var(--ink) }`), alors que le JS pose `--gel` en style INLINE sur chaque bandeau. Une propriété personnalisée déclarée inline l'emporte sur n'importe quelle règle de feuille, quelle que soit sa spécificité : ma surcharge ne pouvait rien. | Pour surcharger un style posé par JS, viser une propriété que le JS ne pose PAS. Un `element.style.setProperty("--x", …)` place la variable au sommet de la cascade pour cet élément : aucune feuille ne la reprendra. Poser la variable en JS et décider en CSS reste le bon partage, mais la décision doit alors s'exprimer sur la propriété FINALE (`background`), pas sur la variable qui l'alimente. Corollaire de vérification : une fonction d'affichage neuve se contrôle sur le RENDU, jamais sur la présence de l'attribut — ici l'attribut était juste, le test d'attente le voyait, et le résultat était faux.
[2026-08-05] | Nathan : « je ne vois pas le logo ». Je l'avais agrandi de 3,6 à 6 mm, sans effet. La cause n'était pas la taille : `static/img/comroster-glyph.svg` est peint en `fill="#EEF1F7"`, un blanc cassé conçu pour le fond SOMBRE de l'écran de régie — sur du papier blanc, il est invisible quelle que soit sa taille. Le fichier voisin `comroster-badge-mono.svg`, dont le nom promettait une variante monochrome, est peint de la même couleur et n'est utilisé nulle part. | Un asset réutilisé dans un contexte de FOND INVERSE doit être vérifié sur sa couleur avant sa géométrie : quand un élément graphique « ne se voit pas », regarder son `fill` avant de toucher à ses dimensions. Et ne jamais se fier au NOM d'un fichier pour croire qu'une variante existe (`-mono`, `-dark`, `-print`) : l'ouvrir. Remède durable ici : poser le glyphe en SVG inline pour qu'il hérite de `currentColor` — un asset qui suit l'encre de son contexte ne peut plus disparaître dans un fond qu'on n'avait pas prévu.
[2026-08-09] | Nathan : « j'aimerais un retour à la page principale quand on clique sur le logo ». Le logo ÉTAIT déjà un lien vers /admin, avec l'infobulle « Retour aux affectations » — il ne marchait pas parce qu'il RECHARGE la page, et que le chargement restaure l'onglet mémorisé en localStorage (ajouté trois semaines plus tôt, pour qu'un rafraîchissement ne perde pas l'onglet). Depuis Impression, cliquer sur le logo ramenait donc sur Impression. Les deux comportements sont justes séparément ; le défaut naît de leur RENCONTRE, et aucun des deux lots ne pouvait le voir. | Quand l'utilisateur demande une fonction qui existe déjà, ne pas la rajouter : chercher pourquoi celle qui existe ne produit pas son effet. Ici la réponse était une fonctionnalité ajoutée APRÈS, qui a silencieusement retourné le sens d'un lien plus ancien. Règle de conception associée : dès qu'un état de navigation devient PERSISTANT (onglet, filtre, tri mémorisés), réexaminer tous les chemins qui rechargent la page — un lien « retour à l'accueil » cesse d'être un retour si l'accueil restaure l'endroit qu'on voulait quitter. Corollaire de test : le témoin qui compte est la persistance elle-même (vérifier que la clé vaut bien « print » avant de cliquer), sinon un simple rechargement suffirait à faire passer le test correctif retiré.
[2026-08-09] | Poussé 18 commits sur `main` avec une suite locale entièrement verte (550 unitaires, 63 e2e, 43 JS, ruff propre) — et la CI est tombée sur deux e2e. Relancée sans AUCUNE modification, elle est passée : c'étaient des tests instables, révélés par un runner plus lent que ma machine. « Vert chez moi » ne disait rien de leur stabilité, seulement de leur réussite une fois. | Un test qui n'a jamais échoué localement n'est pas pour autant stable : la stabilité se mesure par la RÉPÉTITION et sous contrainte de lenteur, pas par une exécution réussie. Deux conséquences pratiques : (1) avant de pousser un lot qui touche aux e2e, les rejouer plusieurs fois — un flaky se cache derrière une seule exécution verte ; (2) quand la CI échoue et qu'un re-run passe, ne pas classer l'affaire « CI capricieuse » mais CONSIGNER le test en cause, parce qu'un échec aléatoire toléré finit par faire ignorer un vrai signal. Indice de diagnostic ici : l'assertion échouait sur une valeur VIDE (`assert 'Régie' in ['']`), signe d'une donnée pas encore arrivée — jamais d'une logique fausse, qui aurait produit une valeur erronée plutôt qu'absente.
[2026-08-09] | `test_l_archive_est_chiffree` cherchait trois secrets en clair dans l'archive, dont le nom de groupe « Son » — TROIS caractères, cherchés dans du base64. Mesuré : un trigramme apparaît par hasard dans ~0,4 % des archives, donc la CI accusait périodiquement le chiffrement de fuir alors qu'il était parfaitement sain. Un test de sécurité qui crie au loup une fois sur deux cent cinquante est pire qu'absent : il apprend à ignorer les alertes de chiffrement. | Chercher une chaîne courte dans un blob CHIFFRÉ ou ENCODÉ ne prouve pas son absence : la probabilité de collision vaut ~L/A^n (L = taille, A = taille de l'alphabet, n = longueur du marqueur) et devient sensible en dessous de 8 signes. Choisir des marqueurs longs et distinctifs — et poser la garde DANS le test (`assert len(marqueur) >= 8`), pour qu'un successeur ne réintroduise pas un nom court sans s'en apercevoir. Corollaire de diagnostic : face à un test qui échoue « au hasard », calculer la probabilité du faux positif avant de soupçonner le code qu'il surveille.
[2026-08-09] | Deux e2e instables, aux symptômes très différents — un rôle vide (`assert 'Régie' in ['']`) et une expiration en attendant un beltpack — avaient la MÊME cause : `page.fill("#person-beltpack", …)` était appelé sans attendre `#person-dialog[open]`. Sur un runner lent, le champ est rempli avant que la boîte ne soit affichée : la saisie part en silence et le formulaire est soumis incomplet, produisant tantôt une personne sans rôle, tantôt aucune personne. Le motif était répété à DOUZE endroits, dans six fichiers. | Avant d'écrire dans un formulaire modal, attendre son OUVERTURE, jamais seulement le clic qui la déclenche : `fill` sur un champ non encore affiché n'échoue pas bruyamment, il produit une donnée partielle — et un test qui soumet un formulaire incomplet échoue plus tard, ailleurs, sur une assertion qui ne parle pas de la vraie cause. Corollaire déjà connu mais re-payé ici : le motif était dupliqué douze fois faute d'un helper commun, donc la correction l'a été aussi. Corollaire de vérification : une seule exécution verte ne prouve pas qu'un flaky est corrigé — rejouer plusieurs fois avant de conclure.
[2026-08-09] | Pour factoriser sept copies d'un helper de test, j'ai écrit une expression régulière de suppression compilée avec `re.S`. Ce drapeau fait matcher `.` À TRAVERS LES LIGNES : le motif, censé retirer une fonction de huit lignes, a mangé 1479 lignes dans sept fichiers. Aucune erreur, aucun avertissement — juste des fichiers vidés de leurs tests. Le seul signal a été un `F401 imported but unused` de ruff sur un import que plus personne n'appelait. | Ne jamais utiliser `re.S` (ni `re.DOTALL`) dans un motif de SUPPRESSION portant sur du code : le point cesse d'être borné à la ligne et la gourmandise devient illimitée. Pour retirer un bloc, raisonner en LIGNES (repérer l'en-tête, avancer tant que la ligne est vide ou indentée, s'arrêter à la première ligne de niveau zéro) — c'est aussi court à écrire et incapable de déborder. Corollaire de sécurité : avant toute réécriture programmatique de masse, vérifier que le travail est COMMITÉ, et contrôler après coup une grandeur invariante (ici `pytest --collect-only` doit rendre le même nombre de tests) plutôt que de relire un diff de 1479 lignes.
[2026-08-09] | Pour garder le correctif du focus différé, j'ai d'abord écrit un test qui « allait vite » au champ rôle après l'ouverture du dialogue. Il passait — et il passait AUSSI avec le défaut réintroduit, donc il ne prouvait rien. Cause : `wait_for_selector` dure bien plus qu'une frame, si bien que le `requestAnimationFrame` fautif s'était déjà exécuté quand le test commençait à agir. Je courais après une course déjà finie. | Une course entre le code de la page et le geste de l'utilisateur ne se teste pas en essayant d'être plus RAPIDE qu'elle : on la RETIENT. Mettre `requestAnimationFrame` (ou `setTimeout`, ou le minuteur en cause) en file d'attente via `add_init_script`, jouer le geste, puis libérer la file — le test devient déterministe et le défaut reproductible à volonté. Ajouter un témoin POSITIF que la file n'est pas vide, sans quoi un minuteur jamais capturé rendrait le test creux. Rappel de la règle qui a permis de s'en apercevoir : une mutation qui ne fait pas tomber son test signale un test creux, jamais un correctif superflu.
